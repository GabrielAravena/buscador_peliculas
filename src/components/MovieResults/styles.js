import { makeStyles } from '@material-ui/styles'

export default makeStyles({
    cardContainer: {
        marginTop: 50,
    },
    poster: {
        width: 200
    },
    titlesContainer: {
        paddingLeft: 50
    }
});